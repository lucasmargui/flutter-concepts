import 'package:flutter/material.dart';

class NavegacaoExemplo3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('NavegacaoExemplo3'),
      ),
      body: Center(
        child: Text('This is NavegacaoExemplo3'),
      ),
    );
  }
}
